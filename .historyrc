# hg <term> [<term> ...] - find history containing all given terms
function hg() {
  if [[ $# -eq 0 ]]; then
    echo "Use: hg <term> [<term> ...]"
    return 1
  fi
  local cmd="history 1"
  local term
  for term in "$@"; do
    cmd+=" | grep -- $(printf '%q' "$term")"
  done
  eval "$cmd"
}
